spring:
  config:
    activate:
      on-profile: local
  data:
    mongodb:
      uri: mongodb://localhost:27017/service-a-db
  kafka:
    bootstrap-servers: localhost:9092
service-b:
  uri: http://localhost:8082
